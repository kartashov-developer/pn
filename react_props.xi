[react] @

Что такое пропсы .
. !Пропсы! - общение между компонентами. Они также могут передавать друг другу данные. 
. !Пропсы! - изначально пустой объект, но при передаче пропсов компоненту, появляются свойства(те самые переданные пропсы).
. Каждый родительский компонент может передать дочернему какую-то информацию.
  . Пропсы могут передавать:
  # примитивные значения(string, number, boolean),
  # объекты,
  # массивы,
  # функции,
  # <JSX>.
Передача пропсов компоненту .
. 